/* Wrapper TU -- see sh_codec.c. Vendored, third-party source; see the
 * Makefile's QUIRC_C99_EXCLUDE / -w note for why this one is NOT built at
 * -std=c99 like every other source/sh_*.c wrapper. */
#include "../../../clients/vendor/quirc/lib/quirc.c"
